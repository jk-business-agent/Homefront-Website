# Netlify build configuration for Homefront Markets.
# Netlify auto-detects Next.js and applies its Next.js runtime.
[build]
  # `npm run build` already runs `prisma generate` before `next build`.
  command = "npm run build"
  publish = ".next"
